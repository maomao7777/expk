##buildroot ext packge to building my system##  
[bind expk to buildroot]:  
./setup_buildroot.sh <extract buildroot folder_name> <board_defconfig>  
cd <extract buildroot folder_name>/expk dir  
you can run  
'make'  
or change .config through 'make <board_defconfig>' or 'make menuconfig'  
